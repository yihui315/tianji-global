<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>预知未来 · 命运已书写</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700&display=swap');

    :root { --vignette-color: rgba(30, 10, 60, 0.78); }

    .hero-container { position: fixed; inset: 0; overflow: hidden; z-index: -2; }

    .hero-video, .tarot-overlay, .zodiac-particles {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .hero-video     { filter: brightness(0.95); z-index: 1; }
    .zodiac-particles { opacity: 0.92; mix-blend-mode: screen; z-index: 3; pointer-events: none; }   /* ← 新增层 */
    .tarot-overlay  { opacity: 0.85; mix-blend-mode: screen; z-index: 4; pointer-events: none; }

    .vignette {
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at center, transparent 38%, var(--vignette-color) 72%);
      pointer-events: none;
      z-index: 5;
      mix-blend-mode: multiply;
      animation: vignette-breath 18s ease-in-out infinite;
    }

    @keyframes vignette-breath { 0%,100% { opacity: 0.92; } 50% { opacity: 1; } }

    /* 其余样式保持不变 ... */
  </style>
</head>
<body class="bg-black text-white overflow-hidden">

  <div class="hero-container" id="heroContainer">

    <!-- 1. 主背景视频 -->
    <video id="heroVideo" class="hero-video" autoplay loop muted playsinline>
      <source src="hero-video.mp4" type="video/mp4">
    </video>

    <!-- 2. 新增：粒子星座连线视频（关键层） -->
    <video id="zodiacParticles" class="zodiac-particles" autoplay loop muted playsinline>
      <source src="zodiac-particles.mp4" type="video/mp4">
    </video>

    <!-- 3. 塔罗卡叠加层 -->
    <video id="tarotOverlay" class="tarot-overlay" autoplay loop muted playsinline>
      <source src="tarot-overlay.mp4" type="video/mp4">
    </video>

    <!-- 4. 水晶球暗角 -->
    <div class="vignette" id="vignette"></div>

    <!-- 内容层（保持不变） -->
    <div class="hero-content px-6"> ...（标题 + CTA 按钮部分不变）... </div>
  </div>

  <script>
    // Boris Skill 封装（新增 /load-zodiac-particles-video）
    function loadZodiacParticlesVideo() {
      const video = document.getElementById('zodiacParticles');
      if (video) {
        video.volume = 0;
        console.log('✅ /load-zodiac-particles-video 已加载（粒子星座连线视频）');
      }
    }

    // initHeroAnimation 更新（已包含新 Skill）
    function initHeroAnimation() {
      createMysticVignette();
      loadTarotOverlay();
      loadZodiacParticlesVideo();        // ← 新增
      cosmicBreathingWobble();
      // （如果你还想保留 Canvas 粒子，可同时调用 addZodiacParticleConstellations()）

      console.log('%c✅ Boris Verification Loop Passed: 粒子星座连线视频已正确加载', 'color:#a78bfa; font-weight:bold');
    }

    window.onload = initHeroAnimation;
  </script>
</body>
</html>
