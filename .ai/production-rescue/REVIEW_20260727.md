# TianJi Production Rescue — Source Review

## Archive verification

- Archive: `tianji-production-preserve-20260727-025136.tar.gz`
- SHA-256: `244e1b3afc422e6a8e15c03495904453518b8a6a2ffc24d59bf3b678960defb6`
- Secret-shape scan from preservation bundle: `0` hits

## Recovered production changes

1. `src/lib/ai-orchestrator.ts`
   - Production-only patch routes the primary `minimax` path through the Anthropic-compatible client.
   - The current main branch already uses the Anthropic-compatible base URL and fallback route, but its primary path still groups MiniMax with OpenAI-compatible providers.
   - Exact patch preserved at `.ai/production-rescue/ai-orchestrator-production.patch`.

2. `src/app/api/subscribe/route.ts`
3. `src/app/api/subscribers/count/route.ts`
   - Both routes were compiled into the active production build and `/api/subscribers/count` returned `3`.
   - The raw hotfix synchronously read and rewrote a fixed JSON file and silently treated corruption as an empty list.

## Adaptation in this PR

- Adds a compatibility-only legacy subscriber store with:
  - environment-overridable path;
  - atomic temp-file plus rename writes;
  - in-process write serialization;
  - case-insensitive duplicate protection;
  - strict store schema validation;
  - no silent conversion of corrupted data into a zero count;
  - staging/degraded mutation guard.
- Adds route and store regression tests.

## Remaining gates

- Apply the exact MiniMax primary-routing patch to `src/lib/ai-orchestrator.ts`.
- Run typecheck, targeted tests, full CI, and degraded staging build.
- Import the three preserved production subscribers into the long-term marketing-leads store before retiring the legacy JSON compatibility layer.
- No production deployment or restart is authorized by this review.
