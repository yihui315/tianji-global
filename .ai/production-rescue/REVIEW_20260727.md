# TianJi Production Rescue — Source Review

## Archive verification

- Archive: `tianji-production-preserve-20260727-025136.tar.gz`
- SHA-256: `244e1b3afc422e6a8e15c03495904453518b8a6a2ffc24d59bf3b678960defb6`
- Secret-shape scan from preservation bundle: `0` hits

## Recovered production changes

1. `src/lib/ai-orchestrator.ts`
   - Production patch routes the primary `minimax` path through the Anthropic-compatible client.
   - The patch is applied in this PR.
   - Resulting Git blob: `42298f00e9355f56cb79cdcd640e817551dc9de6`.
   - Resulting SHA-256: `810048466b27f63b6cba5736808e64325edb639b720449ee76b345ea56e2692a`.
   - Both identities match the preserved production file exactly.

2. `src/app/api/subscribe/route.ts`
3. `src/app/api/subscribers/count/route.ts`
   - Both routes were compiled into the active production build and `/api/subscribers/count` returned `3`.
   - The raw hotfix synchronously rewrote a fixed JSON file and silently treated corruption as an empty list.

## Safe adaptation in this PR

- Adds a compatibility-only legacy subscriber store with:
  - environment-overridable path;
  - atomic temporary-file plus rename writes;
  - in-process write serialization;
  - case-insensitive duplicate protection;
  - strict store schema validation;
  - no silent conversion of corrupted data into a zero count;
  - staging/degraded mutation guard.
- Adds input validation and normalization to `/api/subscribe`.
- Adds explicit no-store and failure behavior to `/api/subscribers/count`.
- Adds seven route and storage regression tests.

## Validation

Evidence: `.ai/production-rescue/STAGING_VALIDATION_20260727.md`

```text
SOURCE_IDENTITY=PASS
TARGETED_TESTS=PASS
TYPECHECK=PASS
DEGRADED_STAGING_BUILD=PASS
DEGRADED_STAGING_AUDIT=PASS
STANDARD_CI=PASS
```

The first staging test attempt correctly failed because the test suite inherited the global subscriber mutation guard. Test environment isolation was fixed without weakening production or staging guards, and the complete validation then passed.

## Remaining production gates

- Preserve and migrate the three existing production subscriber records before retiring the legacy JSON layer.
- Repair production version metadata injection so `/api/version` no longer returns HTTP 500.
- Build an immutable clean release with a traceable commit and validated environment names.
- Start it on an alternate local port and complete API/browser UAT.
- Create and rehearse a real rollback anchor.
- Reconcile the dirty `/opt/tianji-global` worktree only after an approved cutover plan exists.

No production deployment, restart, reload, database mutation, Stripe action, or email send is authorized by this review.

```text
PRODUCTION_READINESS=NO_GO
PRODUCTION_DEPLOY=HOLD
```
